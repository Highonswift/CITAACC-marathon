# ----- Database -----
DATABASE_URL="postgresql://neondb_owner:npg_4OPEJ8kBvzsa@ep-fragrant-forest-ap32l942.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require"

# ----- Public site -----
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# ----- Razorpay -----
# Leave the secret blank to run in MOCK payment mode (no real charge) for local testing.
# Get TEST keys at: Razorpay Dashboard → Settings → API Keys (use rzp_test_* for testing).
# RAZORPAY_KEY_ID and NEXT_PUBLIC_RAZORPAY_KEY_ID should be the SAME public key id.
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
NEXT_PUBLIC_RAZORPAY_KEY_ID=""
# Webhook secret (Dashboard → Settings → Webhooks). Enables reliable server-side
# payment confirmation at /api/payment/webhook. Recommended for production.
RAZORPAY_WEBHOOK_SECRET=""

# ----- Access control (simple passcodes) -----
ADMIN_PASSCODE="citaacc-admin-2026"
VOLUNTEER_PASSCODE="citaacc-volunteer-2026"

# ----- Email (optional, SMTP). Leave blank to log emails to console instead. -----
# Gmail: host=smtp.gmail.com port=587, user=your@gmail.com, pass=APP PASSWORD (not your login).
# SendGrid: host=smtp.sendgrid.net port=587 user=apikey pass=<SG.xxxx>
# Use port 465 for implicit TLS, 587 for STARTTLS.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="citaachennaichapter@gmail.com"
SMTP_PASS="fsdz tvwu jmji tisp"
SMTP_FROM="CITAACC 5K <citaachennaichapter@gmail.com>"
